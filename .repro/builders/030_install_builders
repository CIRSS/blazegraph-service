
export MODULES_DIR=/home/repro/installed-modules
export PACKAGE_SNAPSHOT=/repro/package

envsubst < repro.atstart > /usr/local/bin/repro.atstart
envsubst '$MODULES_DIR $PACKAGE_SNAPSHOT' < repro.install > /usr/local/bin/repro.install

chmod a+rx /usr/local/bin/*
